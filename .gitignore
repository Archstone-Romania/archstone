# Local agent tooling
# Agent definitions, per-agent memory, and local harness settings — not part of the
# shipped product.
.claude/

# Dependencies & build output
node_modules/
dist/
build/
out/
*.tsbuildinfo
coverage/
.turbo/
.cache/

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS & editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
# Everything under this directory ships verbatim to the public repo (release.yml pushes
# `HEAD:archstone` as its own tree), so editor scratch must never become committable here.
*.code-workspace
