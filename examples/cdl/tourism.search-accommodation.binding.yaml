# Implementation binding — NOT CDL
# Links tourism.search-accommodation to a REST backend

binding:
  capabilityId: tourism.search-accommodation
  connector:
    type: rest
    rest:
      baseUrl: "${BOOKING_API_URL}"
      method: POST
      path: /api/v1/hotels/search
